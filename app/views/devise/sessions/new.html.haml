.header-highlight
.w-full.mx-auto.max-w-sm.flex.flex-col.pt-20
  %h1.text-2xl.font-extralight.mb-4 Log in
  = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
    .field
      = f.label :email, class: "font-light"
      %br/
      .flex.border-b.border-secondary.mb-4
        = f.email_field :email, autofocus: true, autocomplete: "email", class: "appearance-none bg-transparent w-full border-none leading-tight focus:ring-0"
    .field
      = f.label :password, class: "font-light"
      %br/
      .flex.border-b.border-secondary.mb-4
        = f.password_field :password, autocomplete: "current-password", class: "appearance-none bg-transparent w-full border-none leading-tight focus:ring-0"
    - if devise_mapping.rememberable?
      .field
        = f.check_box :remember_me
        = f.label :remember_me, class: "font-light"
    .actions.mt-4
      = f.submit "Log in", class: "py-2 px-4 rounded focus:outline-none focus:shadow-outline cursor-pointer bg-gradient-to-r from-cyan-500 to-blue-500 w-full"
